# MapTiler — free, register at https://cloud.maptiler.com (Account -> API keys)
# Serves the OpenStreetMap-based basemap tiles. This key is visible in the browser by design;
# restrict it to your domain in the MapTiler dashboard.
VITE_MAPTILER_KEY=
